require("dotenv").config(); const { createClient } = require("@supabase/supabase-js"); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); supabase.from("all_products").select("id").or("collections.is.null,collections.eq.[]").then(({data, error}) => { if(error) console.error(error); else console.log("Products still needing collections:", data.length); });
